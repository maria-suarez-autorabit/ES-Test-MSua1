<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>nDESIGN__Headless__c</fullName>
    <defaultValue>false</defaultValue>
    <deprecated>false</deprecated>
    <description>If set then the section will be rendered with no header and it will seem as if the section is part of the prior section.</description>
    <inlineHelpText>If set then the section will be rendered with no header and it will seem as if the section is part of the prior section. If used display order is Incremented as 1 instead of 10</inlineHelpText>
    <label>Headless</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
